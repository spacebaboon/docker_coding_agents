# Copy to .env (git-ignored) and customize for your machine.

# Host path to the directory containing your projects. Mounted at /workspace
# inside the container. Can be absolute or relative to docker-compose.yml.
# Default: ../projects
#PROJECTS_DIR=../projects
#GITHUB_PERSONAL_ACCESS_TOKEN=[TOKEN]